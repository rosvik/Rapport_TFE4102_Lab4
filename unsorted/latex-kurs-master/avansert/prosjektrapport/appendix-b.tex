%This is an Appendix
%%=========================================

\chapter{Code}

\begin{lstlisting}[frame=single, language=JavaScript,
    caption=WebSockets Code, label=lst:websockets]
device
  .on('connect', () => {
    console.log('connect');
  });
\end{lstlisting}